import AnimationState from "../../../CoralGameEngine/AnimationState.js";

export default class AnimationStateAtack extends AnimationState{
  constructor(ameObject, image, width, height){
    super(ameObject, image, width, height)
    this.damage = 1
  }

  

}